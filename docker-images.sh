# untuk melihat images yang ada di docker
docker image ls
# untuk download docker images
docker image pull image:tag
# untuk menghapus docker images
docker image rm image:tag
# untuk mengambil informasi dari docker images
docker image inspect image:tag